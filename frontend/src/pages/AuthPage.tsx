import { FormEvent, useState } from 'react'
import { useMutation } from '@tanstack/react-query'
import { Link, useNavigate } from 'react-router-dom'
import { ArrowRight } from 'lucide-react'
import { api } from '../lib/api'
export function AuthPage({mode}:{mode:'login'|'register'}){ const nav=useNavigate(); const [form,setForm]=useState({username:'',email:'',password:''}); const mutation=useMutation({mutationFn:()=>mode==='login'?api.login(form.email||form.username,form.password):api.register(form.username,form.email,form.password),onSuccess:()=>nav('/discover')}); const submit=(e:FormEvent)=>{e.preventDefault();mutation.mutate()}; return <section className="auth-page"><div className="auth-intro"><span className="eyebrow">Every reader has a pattern</span><h1>{mode==='login'?'Welcome back.':'Begin your literary fingerprint.'}</h1><p>No personality quiz. No genre boxes. Just your books, teaching BookDNA what makes you you.</p></div><form onSubmit={submit} className="auth-card"><h2>{mode==='login'?'Sign in':'Create your account'}</h2>{mode==='register'&&<label>Username<input required minLength={3} value={form.username} onChange={e=>setForm({...form,username:e.target.value})} autoComplete="username"/></label>}<label>{mode==='login'?'Email or username':'Email'}<input required type={mode==='register'?'email':'text'} value={form.email} onChange={e=>setForm({...form,email:e.target.value})} autoComplete="email"/></label><label>Password<input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} autoComplete={mode==='login'?'current-password':'new-password'}/></label>{mutation.error&&<p className="error">{mutation.error.message}</p>}<button className="button primary" disabled={mutation.isPending}>{mutation.isPending?'One moment…':mode==='login'?'Sign in':'Create my BookDNA'} <ArrowRight size={17}/></button><p>{mode==='login'?<>New here? <Link to="/register">Create an account</Link></>:<>Already have an account? <Link to="/login">Sign in</Link></>}</p></form></section> }
